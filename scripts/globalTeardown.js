import mongoose from "mongoose";

export default async () => {
  // const uri =
  //   `mongodb://${process.env.MONGO_PATH}/${"bidroom-test"}`;
  // mongoose
  //   .connect(uri, {
  //     useNewUrlParser: true,
  //     useUnifiedTopology: true,
  //     useFindAndModify: true,
  //     useCreateIndex: true,
  //   })
  //   .then((db) => db.connection.dropDatabase());
};
